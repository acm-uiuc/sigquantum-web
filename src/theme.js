import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1a1e24', 
    },
    secondary: {
      main: '#8799b0',
    },
    tertiary: {
      main: '#ad5231',
    },
  },
});

export default theme;

